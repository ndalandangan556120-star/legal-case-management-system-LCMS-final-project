services:
  - type: web
    name: lcms-app
    env: docker
    branch: main
    dockerfilePath: Dockerfile
    startCommand: apache2-foreground
    envVars:
      - key: DOCKER_BUILDKIT
        value: "0"
      - key: BUILDKIT_INLINE_CACHE
        value: "false"
